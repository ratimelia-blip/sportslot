'use client'
import {useState} from 'react';import {supabaseBrowser} from '../../lib/supabase';import Link from 'next/link'
export default function Login(){const sb=supabaseBrowser();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async()=>{setError('');const {error:e}=await sb.auth.signInWithPassword({email,password});if(e){setError(e.message);return}location.href='/dashboard'};return <main><nav className="nav container"><div className="brand">Sport<span>Slot</span></div><Link className="btn" href="/signup">Create club</Link></nav><section className="auth-card"><div className="eyebrow">Club owner</div><h1>Welcome back</h1><p className="muted">Manage bookings, services and availability.</p><div className="form"><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="error">{error}</div>}<button className="btn primary" onClick={submit}>Sign in</button></div></section></main>}
