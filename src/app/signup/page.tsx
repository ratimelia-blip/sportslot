'use client'
import {useState} from 'react';import {supabaseBrowser} from '../../lib/supabase';import Link from 'next/link'
export default function Signup(){const sb=supabaseBrowser();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [club,setClub]=useState('');const [error,setError]=useState('');const [ok,setOk]=useState(false);const submit=async()=>{setError('');if(!name||!email||password.length<6||!club){setError('Please complete all fields. Password must be at least 6 characters.');return}const {data,error:e}=await sb.auth.signUp({email,password,options:{data:{full_name:name,role:'club_owner'}}});if(e){setError(e.message);return}if(data.user){const slug=club.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');const r=await sb.from('clubs').insert({name:club,short_name:club.slice(0,3).toUpperCase(),city:'',slug,owner_id:data.user.id});if(r.error){setError(r.error.message);return}}setOk(true)};return <main><nav className="nav container"><div className="brand">Sport<span>Slot</span></div><Link className="btn" href="/login">Sign in</Link></nav><section className="auth-card"><div className="eyebrow">For club owners</div><h1>Create your club</h1><p className="muted">Start accepting online bookings with SportSlot.</p>{ok?<div className="success"><h2>Account created ✓</h2><p>Check your email if confirmation is required, then sign in.</p><Link className="btn primary" href="/login">Go to login</Link></div>:<div className="form"><input className="input" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)}/><input className="input" placeholder="Club name" value={club} onChange={e=>setClub(e.target.value)}/><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="error">{error}</div>}<button className="btn primary" onClick={submit}>Create account</button></div>}</section></main>}
